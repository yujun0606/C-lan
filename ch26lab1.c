#include <stdio.h>
int main()
{
    int num, max;
    int num1;
    printf("두 수를 입력하세요 : ");
    scanf("%d %d", &num, &num1);
    max = get_max(num, num1);
    printf("큰 값은 %d입니다", max);
    return 0;

}

int get_max(int num, int num1)
{
    if( a > b) return a;
    else return b;

}
